# Benchmark Results

This directory contains ASV benchmark results in JSON format.

Results are organized by machine name and include:
- Benchmark timings for each commit
- Machine specifications
- Environment information

These results are automatically committed by the benchmark workflow.
